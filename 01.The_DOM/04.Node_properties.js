/*
The document node represents the entire HTML document. Its the top-most DOM node; that is, its the parent of all nodes in the DOM. 

HTMLDocument

The HTMLDocument interface, which may be accesed through the Window.HTMLDocument property, extends the Window.HTMLDocument property to include methods and properties that are specific to HTML documents
*/


