/*
DOM nodes have different types:
 - Elements represent HTML tags
 - Text nodes represent text that appears in the document


 Empty nodes contain whitespace
 Empty nodes are not reflected visually in the web browser.
 Empty nodes can appear pretty much anywhere.

*/


// True or False: There is a direct one-to-one mapping between the tags that appear in an HTML file and the nodes in the DOM.

// False, because of empty nodes

// The browser may insert nodes that don't appear in the HTML due to invalid markup or the omission of optional tags. Text, including whitespace, also creates nodes taht don't map to tags.

// True or False: Text nodes sometimes contain nothing but whitespace.

// True. All text -- including whitespace -- in the original HTML appears in the DOM as a text node.

